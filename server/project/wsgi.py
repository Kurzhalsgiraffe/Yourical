from server import app
from server import update_calendars

# update_calendars() # This will start the update when the Server is started. Disabled while Untis is buggy, because server will not start when this crashes!